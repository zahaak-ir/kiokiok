def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('Hy2L.txt', 'Hy2_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('HysteriaL.txt', 'Hysteria_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('juicityL.txt', 'juicity_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('mtprotoF.txt', 'mtproto_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('socksF.txt', 'socks_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('SsL.txt', 'Ss_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('SsrL.txt', 'Ssr_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('TrojanL.txt', 'Trojan_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('VmessL.txt', 'Vmess_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('VlessL.txt', 'Vless_L.txt')

####


def process_ssltxt(input_file, output_file):
    try:
        with open(input_file, 'r') as file:
            lines = file.readlines()
        
        processed_lines = []
        for line in lines:
            if '<br' in line:
                processed_lines.append(line.split('<br')[0].strip() + '\n')
            else:
                processed_lines.append(line)
        
        with open(output_file, 'w') as output:
            output.writelines(processed_lines)
        
        print("Processing complete. Output written to", output_file)
    
    except FileNotFoundError:
        print("Input file not found.")
    except Exception as e:
        print("An error occurred:", str(e))

process_ssltxt('TuicL.txt', 'Tuic_L.txt')

####


